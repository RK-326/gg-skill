# images/ — стиль картинок и генерация

Отвечает на вопрос «в каком стиле картинки и как их делать», чтобы новые
иллюстрации совпадали со старыми, а не выглядели каждый раз по-новому.

## Главные правила процесса
1. Картинки — ОТДЕЛЬНЫЙ этап (этап 5), только после готового текста.
2. НИКОГДА не генерировать по одной через UI. Генерация батчем через API
   (`../scripts/gen_images.py`), сразу много.
3. На каждую метку `[КАРТИНКА: ОПИСАНИЕ]` — 2-3-5 вариантов (примерно одна картинка
   на N страниц).
4. Перед генерацией Claude смотрит в эту папку: на `reference/` (как выглядели
   принятые картинки) и на `prompts/` (какие промты сработали раньше). Каждый новый
   запрос строится на лучшем предыдущем результате — это «компаундинг».
5. Рената отсматривает варианты локально, плохие удаляет, хорошие оставляет.
   Claude вставляет принятые в документ (размеры/рамки/тень — по `../template/design-spec.md`).
6. Принятые варианты и их промты сохраняются обратно в `reference/` и `prompts/` —
   так база с каждым документом обогащается.

## Стиль (фирменный вид GG) — выбран Ренатой
- **Реалистичная фотография с живыми, естественно выглядящими людьми (студентами)**
  в современной обстановке. Фотореализм, мягкий свет, editorial-съёмка. Это утверждённый
  стиль для всех картинок документов (флэт-вариант отклонён).
- Фирменные синие акценты (13445D, 1A5CA8) в одежде и интерьере; обычная современная
  техника, без футуристики/голограмм, без стоковой пошлости.
- **БЕЗ ТЕКСТА на картинке** — ни букв, ни слов, ни подписей, ни цифр (модель их
  коверкает). Весь текст идёт в обычный текст документа.
- Модель: Nano Banana (`google/gemini-2.5-flash-image`).
- Стиль (утверждён Ренатой v2): ЖИВЫЕ искренние эмоциональные моменты, тёплый
  натуральный свет, разные сцены/ракурсы, красивые кампусы американских вузов
  (коллегиальная архитектура, осень) где уместно. НЕ сток, не «человек за ноутом»
  в холодном синем по кругу. Фирменный синий берём из вёрстки, а не форсим в каждое фото.
- ГОЧА letterbox: слово «кинематографичный» в промте заставляет модель добавлять
  ЧЁРНЫЕ ПОЛОСЫ сверху/снизу. Либо не использовать его, либо после генерации обрезать
  полосы (autocrop near-black краёв) и пересчитать высоту картинки в .docx.
- Финально в документе: скругление + рамка D5DBDB + общая мягкая тень.
- РАЗМЕЩЕНИЕ (решение Ренаты, финальное): сгенерированная иллюстрация ставится ТОЛЬКО ОДНА —
  в самом начале документа (hero на титульной странице, после заголовка/подзаголовка).
  В разделах сгенерированных иллюстраций-баннеров БЫТЬ НЕ ДОЛЖНО. Не делать картинку после
  каждого раздела.
- Обучающие СКРИНШОТЫ из оригинала (примеры страниц/сайтов вузов, порталов, форм, заметок
  автора) — это КОНТЕНТ. Их НЕЛЬЗЯ убирать; наоборот, ВСЕ скриншоты оригинала должны
  присутствовать. Если в рабочей версии их не хватает — восстановить из оригинала (PDF/docx)
  в ТОМ ЖЕ порядке, вставляя каждый сразу после его подписи-якоря в тексте
  («…можно найти так:», «Пример вкладки …:», «скриншот ниже», «вот так я записывала…»).
- СТИЛЬ скриншотов как у картинок: скругление (roundRect adj 4200) + рамка D5DBDB + тень,
  по центру. Размер вписать в коробку ~5.5"×3.5" (вертикальные скрины не делать огромными).
- Запрет на удаление касается только СГЕНЕРИРОВАННЫХ декоративных иллюстраций, не скриншотов.
- **Формат — ШИРОКИЙ БАННЕР 16:9 на всю ширину текста** (как в эталоне-бакалавриате),
  НЕ маленький квадрат по центру. Размер в .docx: cx=7223760 EMU, cy≈4063365.
- Гоча генерации: Gemini-image уважает соотношение только во ВЛОЖЕННОМ
  `image_config:{"aspect_ratio":"16:9"}`; top-level `aspect_ratio` он игнорирует
  (отдаёт квадрат 1024×1024). В `gen_images.py` это уже учтено (`--aspect 16:9`).

## Где что лежит
- `reference/` — принятые картинки-образцы (пополняется по мере работы).
- `prompts/` — сохранённые удачные промты (`prompts/starter.md` для старта).
- Диаграммы/таблицы-иллюстрации, которые лучше считать кодом, делает
  `../scripts/gen_illustrations.py` (matplotlib), а не генеративная модель.

## Как сгенерировать (батч)
```bash
cd "$HOME/Documents/Global Generation/gg-skill"
source .env
python3 scripts/gen_images.py --prompt "схема: путь абитуриента от теста до подачи, стиль GG, палитра 13445D/1A5CA8" --n 3 --out images/_drafts
```
Черновики падают в `images/_drafts/` (gitignored). Отсмотрели → лучшее переносим в
`reference/`, промт дописываем в `prompts/`.
